<p id="notice"><%= notice %></p>

<h1>Add Slug To Subscriptions</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_slug_to_subscriptions.each do |add_slug_to_subscription| %>
      <tr>
        <td><%= link_to 'Show', add_slug_to_subscription %></td>
        <td><%= link_to 'Edit', edit_add_slug_to_subscription_path(add_slug_to_subscription) %></td>
        <td><%= link_to 'Destroy', add_slug_to_subscription, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add Slug To Subscription', new_add_slug_to_subscription_path %>
