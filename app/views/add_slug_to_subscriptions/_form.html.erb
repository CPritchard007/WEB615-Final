<%= form_with(model: add_slug_to_subscription, local: true) do |form| %>
  <% if add_slug_to_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_slug_to_subscription.errors.count, "error") %> prohibited this add_slug_to_subscription from being saved:</h2>

      <ul>
      <% add_slug_to_subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
