<%= form_with(model: add_slug_to_publication, local: true) do |form| %>
  <% if add_slug_to_publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_slug_to_publication.errors.count, "error") %> prohibited this add_slug_to_publication from being saved:</h2>

      <ul>
      <% add_slug_to_publication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
